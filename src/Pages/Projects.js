import React from "react";

export const Projects = () => {
    const hello = "HELLO";
    console.log(hello);
    return (
        <div className="text-black">
            {hello}
        </div>
    );
}
